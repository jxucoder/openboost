# Documentation scope

The current documentation source is `docs/v1/`, selected by `mkdocs.yml`.
Other files in this directory document the retired implementation at revision
`50acfc6`; they remain as historical material, not current capability claims.
The new component and recipe documentation will be added alongside implementation.
