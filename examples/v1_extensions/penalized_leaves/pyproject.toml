[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ob-penalized-leaves"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["openboost==1.0.0.dev0", "numpy>=1.24"]

[tool.hatch.build.targets.wheel]
packages = ["src/ob_penalized_leaves"]
