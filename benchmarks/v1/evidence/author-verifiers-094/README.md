# Installed standalone D1/D2 development evidence

Clean source revision **`0a85320ebae7d76faec9f85435bbc41c3f34444a`**. Both standalone
tasks pass in a fresh core-only interpreter: [D1, two cases](D1-result.json) and
[D2, nine cases](D2-result.json). A separate command exits 1 on the intentionally
altered D1 model, with `saved_model: numerical mismatch` in its retained traceback.
This is designer-authored development verification; independent attempts remain
empty and `dispatch_ready` remains false.

The [manifest](manifest.json) retains thirteen exact command invocations/outputs,
all 36 artifact hashes, four tool source hashes, three built wheels and the judge's
installed package identities. It fingerprints 901 NumPy distribution files and
40 OpenBoost distribution files, excluding generated bytecode. The runtime probe
asserts that `ob_expectile`, `ob_cohort_splits` and top-level `tests` are unavailable
in that interpreter. No source checkout or training extension is imported by the
standalone judge.

The evaluator bundle has an explicit public oracle source closure and hashes for
its inputs, expected answers and standalone judge. Both the collector's observations
and all eleven trained models are retained. The negative-control directory is a
copy with one changed model; its failed result is intentional and required.

Reproduction from the recorded revision, using locally cached dependencies:

```bash
UV_CACHE_DIR=/tmp/openboost-research-uv-cache OPENBOOST_BACKEND=cpu \
  uv run --no-sync python -m benchmarks.v1.authoring.verify /tmp/openboost-d1-d2-094-0a85320
```

Use a new output directory when reproducing. The source run wrote outside the
repository so creating evidence did not mark its inputs dirty. Its full output
was copied here unchanged and every artifact hash was rechecked.

Environment: macOS 26.3 / x86_64 runtime, Python 3.12.12, NumPy 2.3.5, OpenBoost
1.0.0.dev0, one requested numerical-library thread, no CUDA. Package builds and
installs use uv offline. Command times are diagnostic, not end-to-end performance
evidence. No tokens, author time, OS access denial, GPU cost or adoption are measured.

The [tooling scope](../../authoring/README.md) describes remaining task checks and
trust assumptions. This artifact closes standalone numerical dependency checks;
it does not replace real accounting/isolation, an incumbent-arm/model/settings
freeze, the pending device run or formal author evaluation.
