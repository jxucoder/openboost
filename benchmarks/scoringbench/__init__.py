"""OpenBoost integration for the external ScoringBench benchmark.

The adapter is not imported here because ScoringBench is an optional external
checkout. Import ``benchmarks.scoringbench.openboost_wrapper`` explicitly after
putting that checkout on ``PYTHONPATH``.
"""
