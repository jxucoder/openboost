name: v1 Publishing Unavailable

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unavailable:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current v1 boundary
        run: |
          echo "The rebuilt package is not release-ready. Implement and verify v1 gates before restoring release publication."
          exit 1
