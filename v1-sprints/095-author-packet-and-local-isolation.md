# Sprint 095: Current author packet and local file isolation

Status: in progress after approval to continue [094](094-author-verifier-preparation.md).
No GPU run, upload, independent model attempt or change to formal budgets.

## Plan and acceptance

1. Refresh the author-view builder around current public CPU documentation and
   the installed core wheel. Record omitted document links explicitly; never
   traverse into known extension solutions, evaluator sources or sealed tasks.
   Preserve the original cfca092 export. Verify all wheel module bytes against
   the source revision and retain failures as incomplete preparation.
2. Add the missing invalid-input/problem-identity development observations for
   D1/D2. Keep their numerical cohort unchanged and reject a missing/false
   rejection observation. These remain designer development checks.
3. Test native macOS file access against a copy of the actual evaluator bundle:
   successful public-wheel work, denied expected-answer/source reads, denied
   evaluator/core writes, inherited child/symlink denial and a forced wall timeout.
   Retain exact commands, raw outcomes and before/after protected hashes.
4. Archive clean-source packet/isolation evidence and reflect. Token accounting,
   model/settings, incumbent paths and actual independent attempts remain open.

First failing checks: current selected docs have links to omitted pages; the
builder does not audit the wheel source closure. The standalone collector lacks
the task cards' invalid-information and foreign-problem rejection observations.

## Execution constraints

`sandbox-exec` could not apply a nested policy inside the current tool sandbox.
The normal tool escalation path allowed a no-op native sandbox capability check.
This permits a bounded local file-access experiment; it does not prove general
host isolation or authorize a model attempt. No security setting is changed.
The native sandbox will constrain only the short-lived probe and its children.

Use the actual immutable evaluator contents copied to a fresh temporary location,
never modify archived evidence in place. The global run-8 freeze remains intact.
No additional numerical fixture catalog, CPU search or synthetic token simulation.

## Results and reflection

The author builder records omitted links, verifies wheel sources and retains
failed builds as incomplete. Verifier v2 adds invalid-input and foreign-problem/
row observations without changing the numerical cohort. Thirty-one focused checks
pass, including deliberately permissive adapters.

The first native policy passed six public examples and thirteen declared checks,
but a separate unlisted answer copy remained readable. The stronger read-allowlist
aborts the interpreter with exit -6 before the first positive example. Selected
system-path probes did not resolve this; the root cause is unproven. This is a
failed isolation gate, not permission to use the weaker policy for author attempts.

Stop native policy expansion at this counterexample. Finish clean-source packet
and verifier reproduction, retain failed isolation commands/profiles, then decide
a supported worker environment and real generated-token accounting. Model/settings
and fair incumbent paths remain unfrozen. No independent attempt or run-8 action.
